# csci201-final-project-backend

Add Firebase key named "ServiceKey.json" to the WebContent folder to run
Currently returns json of all requests in the database, can change to return a single request
